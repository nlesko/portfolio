export enum SkillCategory {
    FRONTEND=1,
    BACKEND=2,
    OTHER=3
}

export enum ProjectCategory {
    PERSONAL = 1,
    PROFESSIONAL = 2,
}

export enum ListStyle {
    GRID = 1,
    LIST = 2,
    TAG = 3,
    CARD = 4,
}
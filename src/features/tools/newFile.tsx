<Fragment>
<div class={`flex flex-wrap ${listStyle === ListStyle.TAG ? 'gap-3' : ''}`}>
{/** \\{tools.length > 0 && tools.map((skill, index) => \\{
        if(skill.isFeatured) \\{
          return (
            <React.Fragment key=\\{skill.id\\}>
              \\{renderListComponent(skill)\\}
            </React.Fragment>
          )
        \\}
        return null;
      \\})
    \\} */}
</div>
</Fragment>;
